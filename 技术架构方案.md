# AI 阅读平台 · 技术架构方案

> 配套文档：《PRD-产品方案.md》。本文聚焦技术实现路径，对应两大功能模块的工程化方案。

---

## 一、整体分层架构

```
┌────────────────────────────────────────────────────────────┐
│                      前端层（Web / 小程序）                    │
│   书库 · 理论图谱可视化 · 案例卡片 · 对话式评测 · 掌握度雷达图   │
└───────────────────────────┬────────────────────────────────┘
                            │ HTTP / WebSocket / SSE（流式）
┌───────────────────────────▼────────────────────────────────┐
│                        API 网关层（BFF）                      │
│   鉴权 · 限流 · 会话管理 · 评测会话状态机                       │
└───────────────────────────┬────────────────────────────────┘
                            │
┌───────────────────────────▼────────────────────────────────┐
│                        核心服务层                             │
│  ┌──────────────┐ ┌──────────────┐ ┌────────────────────┐  │
│  │ 电子书解析服务 │ │ 理论图谱服务  │ │ 问答评测引擎（核心）  │  │
│  └──────────────┘ └──────────────┘ └────────────────────┘  │
│  ┌──────────────┐ ┌──────────────┐ ┌────────────────────┐  │
│  │ 原文定位服务  │ │ 掌握度评估服务 │ │ 圈子话题分析服务      │  │
│  └──────────────┘ └──────────────┘ └────────────────────┘  │
└───────────────────────────┬────────────────────────────────┘
                            │
┌───────────────────────────▼────────────────────────────────┐
│                    基础设施 / 数据层                          │
│  向量库 · 图数据库 · 关系库 · 对象存储 · 大模型（LLM/Embedding）│
└────────────────────────────────────────────────────────────┘
```

---

## 二、核心服务详解

### 2.1 电子书解析服务（离线管道）

**职责**：把原始电子书变成「结构化的、可检索、可引用」的语料。

```
[原始文件] → [格式解析] → [章节切分] → [去噪清洗] → [语义分块] → [向量化入库]
```

| 环节 | 技术方案 | 说明 |
|---|---|---|
| 格式解析 | PDF: pdfplumber / PyMuPDF；EPUB: ebooklib；MOBI: 转换 | 提取正文、标题层级、页码 |
| 章节切分 | 基于目录结构 + 标题正则 | 保留「章-节-段」三级定位 |
| 去噪清洗 | 去除页眉页脚、脚注、图表说明 | 保留段落完整性 |
| 语义分块 | 按语义边界切块（约 512–1024 tokens） | 保证原文段落可作为独立引用单元 |
| 元数据标注 | 提取书名、作者、ISBN、出版信息 | 用于版权校验与正版校验 |

**输出**：结构化的「段落单元」，每个单元携带：`{book_id, chapter, section, paragraph_index, text, page}` —— 这是后续所有「原文引用」的最小单位。

---

### 2.2 理论图谱构建服务（离线 + 大模型）

**职责**：从语料中提炼「核心理论 → 关联学术理论 → 验证逻辑 → 案例」四层结构。

```
[全书语料] ──LLM 分阶段提取──►
  ① 核心理论识别（3–8 个，附定义、原文出处）
  ② 关联学术理论匹配（外部知识库 + LLM 校验，附提出者/年份）
  ③ 验证逻辑链抽取（实验 → 观察 → 推理 → 结论）
  ④ 案例抽取（书内案例 + 检索外部真实案例）
```

**关键技术点**：

1. **幻觉控制（最重要）**：LLM 提炼的每个「核心理论/案例/结论」都**强制携带原文定位引用**（`book_id + chapter + paragraph_index`）。没有原文支撑的提炼结果**不允许入库**。这从根源上避免 AI 编造作者没有的观点。
2. **关联学术理论的外部校验**：不依赖 LLM 的记忆，而是通过**学术知识图谱 / 检索（如 Wikipedia、学术 API、论文库）**交叉验证「这个理论确实存在、由谁提出、哪一年」。
3. **知识存储**：四层关联适合用**图数据库**（Neo4j / NebulaGraph），节点为「理论/实验/案例/章节」，边为「论证/来源/衍生/应用」。

**图模型示例**：

```
(核心理论:前景理论) --[:被挑战]--> (学术理论:期望效用理论)
(核心理论:前景理论) --[:证据]--> (实验:亚洲疾病问题)
(核心理论:框架效应) --[:应用]--> (案例:医疗沟通)
(案例:亚洲疾病问题) --[:原文出处]--> (段落:第34章 第3段)
```

---

### 2.3 问答评测引擎（在线核心服务）

这是产品的**实时核心**，负责「提问 → 判分 → 掌握度 → 补读 → 再提问」闭环。

#### 2.3.1 提问生成

```
输入：核心理论 + 用户历史掌握度 + 已问过的问题
输出：一个「难度适配」的问题
```

- **难度递进**：回忆 → 理解 → 应用 → 迁移（布鲁姆认知目标）。
- **策略**：根据用户当前掌握度动态选题——未掌握时回退到「回忆层」，已部分掌握时上探「应用层」。
- **去重**：避免重复问同一个问题（基于问题向量相似度去重）。
- **圈子视角**：用户选了某圈子（如「HR 圈」），提问会代入该视角（如「在面试场景下，如何规避锚定效应？」）。

#### 2.3.2 答案判分

```
输入：用户答案 + 标准答案要点（由理论图谱中的"验证逻辑/结论"生成）
输出：{ 评级: ✅/🟡/❌, 三维护分, 反馈文本 }
```

- **评分 Rubric 结构化**：要求 LLM 按「准确性 40% / 完整性 30% / 迁移性 30%」三个维度分别打分，而非笼统给分，提升一致性。
- **锚定标准答案**：标准答案要点来自已入库的理论图谱，而非 LLM 自由发挥。
- **反馈生成**：指出「答对了什么 / 漏了什么 / 偏差在哪」，并**不直接给出完整答案**（保留二次学习空间）。

#### 2.3.3 原文推荐（补读分支）

```
触发条件：同一核心理论累计 2–3 次「未掌握」
动作：从向量库检索与该"理解缺口"最相关的原文段落 → 推送
```

- **缺口定位**：LLM 判断用户「具体卡在哪个理解点上」（如「混淆了损失厌恶和风险规避」），据此检索针对性段落。
- **原文最小剂量**：只推 1–3 段强相关原文（含章节定位），不推整章。
- **换问法再考**：用户读完点「我已读完」后，AI 生成一个**不同角度**的等价问题，验证真实掌握（防止背答案）。

#### 2.3.4 会话状态机

```
状态: 初始 → 提问 → 等待作答 → 判分 → [已掌握 → 下一理论] / [部分 → 追问] / [未掌握 → 计数+1 → (计数≥阈值 → 原文推荐 → 等待阅读 → 换问再考)]
```

每个核心理论维护独立的：`掌握度(0–100%)`、`连续未掌握次数`、`已问问题列表`。

---

### 2.4 掌握度评估服务

| 功能 | 实现 |
|---|---|
| 单理论掌握度 | 三次作答判分的加权滚动（近期权重更高） |
| 全书画像 | 各理论掌握度 + 各维度（准确/完整/迁移）雷达图 |
| 阈值判定 | ≥80% 移出「待掌握」，50–79% 巩固，<50% 补读 |
| 报告生成 | LLM 汇总生成个人掌握度报告 |

---

### 2.5 圈子话题分析服务（半离线）

```
输入：核心理论列表
输出：按圈子分组的「讨论话题」
```

- 用 LLM + 检索，把每个核心理论映射到「哪个圈子在讨论、讨论什么」。
- 话题数据沉淀为**标签库**，供前端「圈子标签页」与提问视角切换使用。
- 热度数据可接入公开内容源（如论文引用量、社区讨论热度）做参考。

---

## 三、检索增强（RAG）方案

| 组件 | 选型建议 | 说明 |
|---|---|---|
| Embedding 模型 | text-embedding-3-large / bge-m3 | 段落级向量化 |
| 向量库 | Milvus / pgvector / Qdrant | 支持按 book_id 过滤 + 语义检索 |
| 检索策略 | Hybrid（向量 + 关键词 BM25） | 兼顾语义与术语精确匹配 |
| 重排 | rerank 模型（如 bge-reranker） | 提升原文推荐的精准度 |
| 引用约束 | 检索结果必须带 `book_id + chapter + paragraph` 元数据 | 保证可回溯、可防幻觉 |

---

## 四、大模型调用策略

| 场景 | 模型要求 | 备注 |
|---|---|---|
| 书籍解析（提炼理论/逻辑链/案例） | 强推理长文本（如 GPT-4 级 / Claude / 国产 DeepSeek-R1、Qwen-Max） | 离线批处理，可异步 |
| 提问生成 + 判分 | 中等模型 + 结构化输出（JSON Schema） | 在线，需低延迟，用 SSE 流式 |
| Embedding | 专用 embedding 模型 | 与 LLM 分离 |
| 原文推荐检索 | 向量检索 + rerank | 无需生成模型 |

**工程要点**：
- 判分、提问等**结构化输出**用 `JSON Schema / Function Calling` 约束格式。
- 在线链路用**流式输出（SSE）**提升对话体验。
- 解析等重任务用**异步任务队列**（如 Celery / BullMQ）解耦。

---

## 五、技术选型总览

| 层 | 推荐选型 |
|---|---|
| 前端 | React / Vue + 可视化（ECharts / D3 做理论图谱） |
| 后端 | Python（FastAPI）为主，辅以 Node（BFF） |
| 图数据库 | Neo4j / NebulaGraph（理论图谱四层关联） |
| 向量库 | Milvus / pgvector |
| 关系库 | PostgreSQL（用户、书目、评测记录） |
| 对象存储 | 对象存储（电子书原件、解析产物） |
| 任务队列 | Celery / BullMQ（离线解析） |
| LLM 网关 | 统一模型网关，支持多模型路由与降级 |

---

## 六、MVP 落地路径（建议分期）

**Phase 1（0–1，验证闭环）**
- 电子书解析（PDF/EPUB）+ 语义分块入库
- LLM 提炼核心理论 + 关联理论 + 逻辑链 + 案例（半自动，人工抽检）
- 对话式提问 + 判分 + 掌握度 + 原文推荐（单书《思考，快与慢》打磨到好用）

**Phase 2（扩内容）**
- 理论图谱图数据库化、图谱可视化
- 圈子话题分析、多视角提问
- 覆盖 20–50 本高价值经典书

**Phase 3（规模化 + 数据飞轮）**
- 开放用户上传/长尾书自动解析
- 判分模型用真实「错题+补读」数据持续微调
- B 端团队学习看板

---

## 七、关键技术风险与对策

| 风险 | 对策 |
|---|---|
| LLM 幻觉（编造理论/案例） | 强制原文引用锚定；无出处不入库；核心书目人工抽检 |
| 判分一致性差 | 结构化 Rubric + JSON 输出约束 + 抽样人工校准 + 数据微调 |
| 原文推荐不准 | Hybrid 检索 + rerank；按「理解缺口」定向检索而非泛检索 |
| 长书解析成本高 | 异步批处理 + 缓存解析产物 + 热门书预解析 |
| 版权 | 仅截取最小必要段落；校验用户已拥有该书；对接正版书库 |
